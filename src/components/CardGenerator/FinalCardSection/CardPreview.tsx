import { CardPreviewProps } from '../../../types/card.types';

const CardPreview: React.FC<CardPreviewProps> = ({ image, details }) => {
    return <div>CardPreview Component (Coming Soon)</div>;
};

export default CardPreview;